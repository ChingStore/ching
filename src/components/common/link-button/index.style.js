import STYLE from 'constants/style'

export default {
  base: {
    display: 'inline-flex',
    alignItems: 'center',
    justifyContent: 'center',

    flexShrink: 0,
    color: STYLE.COLOR.BLUE,
    fontWeight: 600,
    fontSize: 16,
  },
}
