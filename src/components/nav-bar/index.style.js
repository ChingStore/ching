// @flow

import STYLE from 'constants/style'

export default {
  base: {
    borderBottom: `1px solid ${STYLE.COLOR.LIGHT_BLUE}`,
  },

  tab: {
    flexGrow: 1,

    height: 64,

    alignItems: 'center',
    justifyContent: 'center',
  },
  tab_text: {
    marginLeft: 10,
  },
}
